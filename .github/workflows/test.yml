name: Testing

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: artis3n/ansible_galaxy_collection@1f0f283dd713bec80058a30d3f1300dc274162b3
        with:
          api_key: ${{ secrets.GALAXY_API_KEY }}
